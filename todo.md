# Todo

- Resolve the existing project-wide TypeScript errors reported by `npm run typecheck` in unrelated account, analytics, feed, Prisma, and service files.
- Complete the data-store migration: existing service façades and infrastructure files still import the deleted `core.v2/lib/data-store` module.
